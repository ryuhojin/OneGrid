# F-SELECT Selection

Selection demonstrates the shared core selection model across row, cell, range, merged-cell, visible-row, and server-dataset token flows.

The vanilla example is the executable reference used by Playwright. React and Vue pass the same `GridOptions.selection` object to the DOM renderer without reimplementing selection behavior.
