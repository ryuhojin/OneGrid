# ROW-001 Client Row Model

This example renders client-side row identity, transactions, filtering, sorting, grouping, and
aggregation through the shared `@onegrid/core` `ClientRowModel`.

The DOM, React, and Vue surfaces pass the same `GridOptions` model inputs and do not reimplement row
logic in wrappers.
