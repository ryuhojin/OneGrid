# F-CLIP Clipboard

Clipboard demonstrates text/plain copy and paste across selected cells, selected rows, optional headers, validation, and merge-aware blank cells.

The vanilla example is the executable reference used by Playwright. React and Vue pass the same `GridOptions.clipboard` contract to the DOM renderer without reimplementing clipboard behavior.
