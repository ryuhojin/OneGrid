# DOM-003 Accessibility

This example validates the shared accessibility contract across vanilla, React, and Vue.
It exercises grid labeling, live region text, ARIA row and cell coordinates, column menu
semantics, keyboard focus, merged cells, and pinned panes.
