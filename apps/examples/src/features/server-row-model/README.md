# ROW-003 Server Row Model

This example verifies that OneGrid sends sorting, filtering, grouping, aggregation, and pivot
models to a `DataSource` instead of applying those operations through the client row pipeline.

The vanilla example also exercises server cache refresh and transaction updates through the DOM
bridge.
