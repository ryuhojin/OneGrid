# ROW-004 Viewport Row Model

This example renders a small synchronized viewport over 10M logical rows. Rows are generated by a
`DataSource` only for the requested viewport range; no full client array is created.

The vanilla example exposes viewport jump and live update actions for E2E coverage.
