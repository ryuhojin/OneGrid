# THEME-001 Theme Foundation

This example exercises OneGrid theme tokens, runtime theme switching, density switching, and scoped
CSS variable overrides. The BNK scoped option demonstrates per-grid variables without global CSS.
