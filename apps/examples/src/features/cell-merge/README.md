# Cell Merge Layout

`LAY-004` demonstrates the first cell merge layout contract:

- value merge on contiguous `Region` and `Agency` cells
- custom horizontal merge on the `Memo` column
- server supplied `mergeMeta` on the pinned `Status` column
- pinned pane and visible window clipping through the DOM renderer
