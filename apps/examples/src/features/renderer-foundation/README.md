# Renderer Foundation

Renderer foundation verifies the DOM renderer hosts used by cells, group headers, overlays, and
batched invalidation. The example keeps string output text-safe by default and enables sanitized
HTML explicitly through the grid security option.
