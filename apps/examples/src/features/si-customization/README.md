# THEME-002 SI Customization

This example maps SI design-system tokens into OneGrid CSS variables through
`createSiTheme()`. It demonstrates tenant palette switching, density switching, pinned panes, group
headers, and scoped runtime theme variables.
