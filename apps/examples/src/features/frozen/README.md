# Frozen Rows / Columns

Demonstrates top and bottom frozen rows with left and right frozen columns. The example keeps
the scrollable body virtualized and uses value-based cell merge across frozen and scrolling
sections.
