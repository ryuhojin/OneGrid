# Group Header

This example exercises the header tree and matrix contract:

- nested group headers
- explicit top-row header merge
- pinned left and right clipping in the core header region model
- header ARIA labels
