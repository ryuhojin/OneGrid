# Sorting

This example exercises interactive sorting across the DOM renderer and framework wrappers:

- header click and keyboard sort toggling
- ascending, descending, and unsorted cycle
- Shift-click multi sort
- custom status comparator
- pinned status column with sort state rendering
