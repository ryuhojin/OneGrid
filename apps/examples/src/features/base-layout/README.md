# LAY-001 Base Layout

This example exercises the base renderer layout contract:

- root grid layout with explicit sizing
- header, body, summary, and footer sections
- pinned left, center, and right panes
- overlay layer host
- resize observer host metadata
