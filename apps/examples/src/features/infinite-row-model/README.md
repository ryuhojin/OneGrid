# ROW-002 Infinite Row Model

This example uses a server-like `DataSource` and `InfiniteRowModel` block requests instead of a full
client array. Rows are generated only for requested blocks, with skeleton entries shown while a block
is loading.
