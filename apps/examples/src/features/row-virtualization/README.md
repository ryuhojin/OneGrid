# LAY-002 Row Virtualization

This example validates fixed row-height virtualization, overscan limits, `scrollToRow`, and
segmented scroll configuration without creating a browser DOM node for every logical row.
