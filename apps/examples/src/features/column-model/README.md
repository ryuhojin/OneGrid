# Column Model

This example exercises the core column model through the DOM renderer:

- normalized column ids
- ordered visible leaf columns
- hidden columns
- pinned left and right leaf columns
- width, min width, and flex metadata
