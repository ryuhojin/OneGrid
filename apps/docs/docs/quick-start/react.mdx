# React Quick Start

```tsx
import { OneGrid } from "@onegrid/react";

export function OrdersGrid() {
  return <OneGrid columns={[{ field: "id" }]} data={[{ id: "ORD-1001" }]} />;
}
```

For ref, event prop, and React renderer bridge details, see
[React Wrapper](../frameworks/react).
