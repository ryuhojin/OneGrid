# Installation

The package names are reserved by the monorepo baseline:

```bash
pnpm add @onegrid/core @onegrid/dom @onegrid/themes
```

Published npm and CDN install instructions will be completed during packaging hardening.
