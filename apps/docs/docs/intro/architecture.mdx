# Architecture

OneGrid starts with a DOM-free `@onegrid/core`, a browser renderer in `@onegrid/dom`, and thin
React/Vue wrappers. Large data support is designed around server, infinite, and viewport row models
instead of loading every row into the browser.
